# R/utility_functions.R

# 定义自定义函数，例如 plot_map, plot_bee_distr 等

# 示例：plot_map 函数
plot_map <- function(fill, geodat, idx, ol, is_all, add_poly = FALSE, us_proxy = NULL, zcta_choose = NULL) {
  # 绘制地图的逻辑
  # 这里根据您的实际需求实现地图绘制
  # 返回 leaflet 对象
}

# 示例：plot_bee_distr 函数
plot_bee_distr <- function(fill, st, mwi, idx, ol, is_all, hl, zcta_hl) {
  # 绘制分布图的逻辑
  # 使用 ggplot2 和 plotly 生成交互式图表
  # 返回 plotly 对象
}
